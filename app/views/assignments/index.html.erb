<div class="container">
  <p id="notice"><%= notice %></p>

  <h1>Homework Assignments</h1>
<div class="filter">
<h2>Filter Assignments By:</h2>
<%= search_form_for @search do |f| %>
<label>
<%= f.radio_button :status_cont, "Open", checked: "checked" %>
  Open Assignments (Displayed by default)
</label>
  &nbsp
&nbsp
  <label>
  <%= f.radio_button :status_cont, "Upcoming" %>
  Upcoming Assignments
</label>
  &nbsp
&nbsp
 <label>
  <%= f.radio_button :status_cont, "Closed" %>
  Closed Assignments
</label>
   
  <br>
   <br><br>

<div class="actions">
  <%= f.submit "Filter", class: "btn btn-green form-control" %>  
</div>
   </div>

<% end %>
  <table class="table table-striped table-responsive">
    <thead>
      <tr>
        <th>Name</th>
        <th>Description</th>
        <th>Due Date</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @assignments.each do |assignment| %>
        <tr>

         <% if assignment.assignment_type == "Main" %>
                  <td style ="background: #16a085 !important; font-size: 1.1rem !important; text-align: center; color: white !important" class="lesson_type"><%= link_to assignment.name, assignment %></td>
                  <% end %>
              <% if assignment.assignment_type == "Lab" %>

                 <td style ="background: #c0392b !important; font-size: 1.1rem !important; text-align: center; color: white !important" class="lesson_type"><%= link_to assignment.name, assignment %></td>

                <% end %>
         
          <td><%= assignment.description %></td>
          <td><%= assignment.deadline %></td>
          <td><%= link_to 'Show', assignment %></td>
        <% if current_user.try(:admin?) %>
          <td><%= link_to 'Edit', edit_assignment_path(assignment) %></td>
          <td><%= link_to 'Destroy', assignment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>
  <% if current_user.try(:admin?) %>

  <%= link_to 'New Assignment', new_assignment_path %>
<% end %>
</div>
